name: appsignal
description: apm, metrics
clients:
  codex:
    command: docker run -i --rm -e APPSIGNAL_API_KEY -e USER_AGENT appsignal/mcp:local
    env_keys: ["APPSIGNAL_API_KEY", "USER_AGENT"]
  claude:
    command: docker run -i --rm -e APPSIGNAL_API_KEY -e USER_AGENT appsignal/mcp:local
    env_keys: ["APPSIGNAL_API_KEY", "USER_AGENT"]
  goose:
    command: docker run -i --rm -e APPSIGNAL_API_KEY -e USER_AGENT appsignal/mcp:local
    env_keys: ["APPSIGNAL_API_KEY", "USER_AGENT"]
