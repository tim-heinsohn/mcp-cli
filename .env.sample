# Copy this file to `.env` and customise values as needed. The wrapper at
# ~/mcp/bin/appsignal-mcp-wrapper (and n8n counterpart) will source it automatically.

# Command used to retrieve the AppSignal API key when it is not already present
# in the environment. The command should print the key to stdout and may be a
# script living in your dotfiles.
APPSIGNAL_API_KEY_HELPER="~/ia.dotfiles/bin/appsignal_api_key_helper"

# Command that prints `KEY=VALUE` lines for required n8n credentials when they
# are not already available in the MCP environment.
N8N_API_ENV_HELPER="~/ia.dotfiles/bin/n8n_api_env_helper"
