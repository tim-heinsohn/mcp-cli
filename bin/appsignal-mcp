#!/usr/bin/env bash
set -euo pipefail
: "${APPSIGNAL_API_KEY:?APPSIGNAL_API_KEY is required}"

args=(run -i --rm -e APPSIGNAL_API_KEY)
if [[ -n "${APPSIGNAL_ENDPOINT:-}" ]]; then
  args+=( -e APPSIGNAL_ENDPOINT )
fi
args+=( appsignal/mcp )

exec docker "${args[@]}"
